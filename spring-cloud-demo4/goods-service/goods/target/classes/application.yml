server:
  port: 7001

spring:
  application:
    name: goods

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true